<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto" 
>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- WorkManager runs the Ark background-refresh worker as a foreground
         service so its process boots at IMPORTANCE_FOREGROUND_SERVICE. RN
         0.76 New Arch's DefaultNewArchitectureEntryPoint.load() init does
         not complete in a no-Activity service-only process — the worker's
         brief foreground status (~10-30s while a refresh round is in flight)
         lets that init complete and the headless JS task start. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- BOOT_COMPLETED + MY_PACKAGE_REPLACED are sent by the system so the
         Ark background-refresh AlarmManager alarm can be re-armed after
         a reboot or app update. Without this permission the receiver
         won't fire and the cadence dies on the first reboot. See
         ArkRefreshBootCompletedReceiver. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACTION_OPEN_DOCUMENT" />
    <uses-permission android:name="android.permission.ACTION_GET_CONTENT" />
    <uses-permission android:name="android.permission.ACTION_CREATE_DOCUMENT" />

    <application
        android:name=".MainApplication"
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:icon="@mipmap/logonew"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/logonew"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="false"
        tools:replace="android:usesCleartextTraffic">

        <meta-data
            android:name="com.dieam.reactnativepushnotification.notification_channel_name"
            android:value="BlueWallet notifications" /> <!-- YOUR NOTIFICATION CHANNEL NAME -->
        <meta-data
            android:name="com.dieam.reactnativepushnotification.notification_channel_description"
            android:value="Notifications about incoming payments" /> <!-- YOUR NOTIFICATION CHANNEL DESCRIPTION -->

        <!-- Change the value to true to enable pop-up for in foreground (remote-only, for local use ignoreInForeground) -->
        <meta-data
            android:name="com.dieam.reactnativepushnotification.notification_foreground"
            android:value="true" />
        <!-- Change the value to false if you don't want the creation of the default channel -->
        <meta-data
            android:name="com.dieam.reactnativepushnotification.channel_create_default"
            android:value="true" />
        <!-- Change the resource name to your App's accent color - or any other color you want -->
        <meta-data
            android:name="com.dieam.reactnativepushnotification.notification_color"
            android:resource="@color/white" /> <!-- or @android:color/{name} to use a standard color -->

        <receiver android:name="com.dieam.reactnativepushnotification.modules.RNPushNotificationActions" />
        <receiver android:name="com.dieam.reactnativepushnotification.modules.RNPushNotificationPublisher" />
        <receiver
            android:name="com.dieam.reactnativepushnotification.modules.RNPushNotificationBootEventReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.dieam.reactnativepushnotification.modules.RNPushNotificationListenerService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Headless JS task host for the Ark background-refresh.
             Started from ArkRefreshAlarmReceiver via startForegroundService
             and promoted to FGS in onStartCommand. dataSync
             foregroundServiceType pairs with the FOREGROUND_SERVICE_DATA_SYNC
             permission and is required on Android 14+ to call
             startForeground. Not exported — only our own receiver triggers
             it. -->
        <service
            android:name=".ArkRefreshHeadlessTaskService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Alarm-fired receiver that hosts the actual cold-process wake.
             AlarmManager-fired receivers get a brief allowlist exemption
             that lets them call startForegroundService(). See receiver
             class doc + ArkBackgroundSchedulerModule for the full
             rationale (Android 14 FGS-from-background restrictions). -->
        <receiver
            android:name=".ArkRefreshAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.cypherbox.btc.ARK_REFRESH_ALARM" />
            </intent-filter>
        </receiver>

        <!-- Re-arm the Ark refresh alarm after device boot or app
             upgrade. AlarmManager state lives only in RAM, so a reboot
             would otherwise silently kill the cadence. exported="true"
             is required: BOOT_COMPLETED + MY_PACKAGE_REPLACED are
             system-broadcast and the system delivers them only to
             receivers visible across the package boundary. The receiver
             is a no-op when the SharedPreferences flag says the user
             hasn't enabled auto-refresh. See receiver class doc. -->
        <receiver
            android:name=".ArkRefreshBootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Declare foregroundServiceType on WM's SystemForegroundService
             so ArkRefreshWorker.setForegroundAsync() can elevate the
             worker to a foreground service. Without this Android 14+
             throws IllegalArgumentException ("foregroundServiceType
             0x00000001 is not a subset of foregroundServiceType
             attribute 0x00000000") because WM's library manifest
             declares the service with no type. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Disable WorkManager's eager auto-initialization via
             androidx.startup. Pairs with MainApplication implementing
             Configuration.Provider for on-demand init. Without this,
             cold-process wakes spawned for SystemJobService have WM's
             reconciliation cancel the very dispatch that woke us. See
             MainApplication.getWorkManagerConfiguration() comment. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

<!--        <activity-->
<!--            android:name=".SplashActivity"-->
<!--            android:exported="true">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <action android:name="android.intent.action.VIEW" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->


        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/BootTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="bitcoin" />
                <data android:scheme="lightning" />
                <data android:scheme="cypherbox" android:host="oauth/callback" />
                <data android:scheme="lapp" />
                <data android:scheme="blue" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />

                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:mimeType="application/octet-stream"
                    android:pathPattern=".*\\.psbt" />
            </intent-filter>
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />

                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:mimeType="text/plain"
                    android:pathPattern=".*\\.psbt" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="com.bugsnag.android.API_KEY"
            android:value="17ba9059f676f1cc4f45d98182388b01" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>
